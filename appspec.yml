version: 0.0
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
hooks:
  ApplicationRestart:
    - location: Scripts/restart_httpd.sh
      timeout: 300
      runas: root
